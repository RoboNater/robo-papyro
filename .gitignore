# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
sample-pdfs

# Coverage data: a binary artifact regenerated by every test run.
.coverage
.coverage.*

# The drop point for real house templates during manual work (rp-docx-spec §11.1).
# Nothing here is ever required for CI, and a corporate .dotx must not be
# committed by accident.
templates/local/
